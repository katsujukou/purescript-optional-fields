package:
  name: "record-ptional-fields"
  publish:
    version: 0.1.0
    location:
      githubOwner: katsujukou
      githubRepo: purescript-record-optional-fields
    license: MIT
  dependencies:
    - maybe
    - prelude
    - record
    - type-equality
    - unsafe-coerce
  test:
    main: Test.Main
    dependencies:
      - aff
      - console
      - effect
      - spec

workspace:
  lock: true
